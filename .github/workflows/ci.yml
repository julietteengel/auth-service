name: Pipeline de CI - auth-service

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    uses: FIAP-Teach-Challenge-2/toggle-master-ci/.github/workflows/reusable-ci-go.yml@main
    with:
      service-name: auth-service
      ecr-repository: togglemaster/auth-service
    secrets: inherit
